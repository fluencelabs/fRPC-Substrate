aqua Deals declares *

data Deal:
    chainNetwork: string
    chainNetworkId: u64
    dealId: string
    dealIdOriginal: string
    definition: string
    timestamp: string

data Deals:
    fRpcDeployment: ?Deal

func get() -> Deals:
    <- Deals(
        fRpcDeployment=nil
    )
